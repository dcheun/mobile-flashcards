import React from "react";
import { View, Text, StyleSheet } from "react-native";

const QuizScreen = () => {
  return <Text style={{ fontSize: 48 }}>QuizScreen</Text>;
};

const styles = StyleSheet.create({});

export default QuizScreen;
